.locations {
  @apply -mx-12 flex flex-wrap w-full justify-center items-center;
  max-width: 100%;
  overflow: hidden;

  @media (max-width: 680px) {
    @apply mx-0;
  }

  h3 {
    @apply w-full;
  }

  &__single {
    @apply mx-12 flex mb-16 flex-col shadow-lg p-8 rounded justify-center items-center;

    @media (max-width: 680px) {
      @apply mx-0;
    }

    &--thumbnail {
      @apply relative;
      border-radius: 50%;
      overflow: hidden;
      height: 300px;
      width: 300px;
      @media (max-width: 680px) {
        width: 200px;
        height: 200px;
      }
      img {
        @apply w-full h-full absolute top-0 left-0;
        object-fit: cover;
      }
    }

    &--wrapper {
      border: 2px solid black;
      @apply flex flex-col justify-center items-center border-gray p-24;

      @media (max-width: 680px) {
        @apply p-8;
      }
    }

    h4 {
      @apply flex flex-col items-center mb-0 justify-center font-josefin uppercase text-3xl relative;
    }

    .seperator {
      @apply relative flex flex-col justify-center items-center text-2xl text-gray;

      &:before,
      &:after {
        @apply relative flex bg-gray my-4;
        content: "";
        height: 15px;
        width: 2px;
      }

      &:before {
        @apply mb-2;
      }

      &:after {
        @apply mt-2;
      }
    }

    time {
      @apply font-freight text-3xl mb-2;
    }

    p {
      @apply text-center font-freight text-2xl my-0;
      color: "#1C1C1CE6";
    }

    a {
      @apply text-center relative mt-4 text-xl font-freight text-gray font-bold;
      text-decoration: none;
      transition: 0.4s ease-in-out;

      &:after {
        @apply flex absolute bg-gray;
        height: 2px;
        width: 0;
        content: "";
        top: 100%;
        transition: 0.4s ease-in-out;
      }

      &:hover {
        @apply text-slate;

        &:after {
          @apply bg-slate w-full;
        }
      }
    }
  }
}
