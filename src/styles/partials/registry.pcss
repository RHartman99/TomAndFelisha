.registry {
  @apply w-full flex flex-col;

  h3 {
    @apply mb-0;
  }

  &__iframe {
    @apply w-full flex flex-col;
    overflow: hidden;

    iframe {
      margin-top: -6rem;

      .btnViewOrBuy {
        @apply bg-black !important;
      }
    }
  }
}
